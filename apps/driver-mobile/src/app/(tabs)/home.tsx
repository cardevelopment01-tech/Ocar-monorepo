import { Text, View } from 'react-native'

// Placeholder — online/offline toggle lands days 3-4 (shell), background location days 8-10.
export default function HomeScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Driver — Home (placeholder)</Text>
    </View>
  )
}
