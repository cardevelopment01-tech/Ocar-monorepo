# Used automatically for release builds (NODE_ENV=production) -- local dev
# still uses .env (localhost via adb reverse). See CLAUDE.md's "Pending Ops
# Actions" for staging vs prod tradeoffs if this ever needs to change.
EXPO_PUBLIC_API_URL=https://ocar-api.clienttesting.in
