events {
  worker_connections 1024;
}

http {
  client_max_body_size 20m;
  # Redirect all HTTP to HTTPS
  server {
    listen 80;
    server_name _;

    # Let's Encrypt ACME challenge
    location /.well-known/acme-challenge/ {
      root /var/www/certbot;
    }

    location / {
      return 301 https://$host$request_uri;
    }
  }

  # HTTPS — API
  server {
    listen 443 ssl;
    server_name ocar-api.clienttesting.in;

    ssl_certificate     /etc/letsencrypt/live/ocar-api.clienttesting.in/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/ocar-api.clienttesting.in/privkey.pem;

    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    # Security headers
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Frame-Options DENY always;
    add_header X-Content-Type-Options nosniff always;

    # REST API
    location /api/ {
      # Duplicate of the app's own pino request log (same IP/method/status,
      # just unstructured) — both get tailed into the same Loki stream by
      # Alloy, so this was doubling line count with no extra signal.
      access_log off;
      proxy_pass         http://api:4000;
      proxy_http_version 1.1;
      proxy_set_header   Host $host;
      proxy_set_header   X-Real-IP $remote_addr;
      proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header   X-Forwarded-Proto $scheme;
      proxy_cache_bypass $http_upgrade;

      # Increase timeout for long-running requests
      proxy_read_timeout 60s;
    }

    # Socket.io — WebSocket upgrade
    location /socket.io/ {
      proxy_pass         http://api:4000;
      proxy_http_version 1.1;
      proxy_set_header   Upgrade $http_upgrade;
      proxy_set_header   Connection "upgrade";
      proxy_set_header   Host $host;
      proxy_set_header   X-Real-IP $remote_addr;
      proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header   X-Forwarded-Proto $scheme;
      proxy_cache_bypass $http_upgrade;
      proxy_read_timeout 86400s;
    }

    # Health check endpoint
    location /health {
      proxy_pass http://api:4000;
      access_log off;
    }
  }
}
